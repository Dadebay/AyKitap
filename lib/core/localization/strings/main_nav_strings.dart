/// Strings for [MainNavScreen] and its [_WheelNavBar].
///
/// The wheel nav bar is icon-only (no text labels, tooltips, or other
/// user-visible strings), so there is currently nothing to externalize here.
/// This class exists so the module still follows the standard per-module
/// `XxxStrings` pattern, ready for the day a text label is added.
class MainNavStrings {
  MainNavStrings._();
}
