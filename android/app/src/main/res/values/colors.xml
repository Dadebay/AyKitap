<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Light launch background (matches AppColors.bg in light mode), used
         when the OS is in light mode. MainActivity overrides the window
         background with the app's own stored theme as soon as it starts, so
         the manual in-app light/dark switch wins over the OS setting. -->
    <color name="splash_bg">#F6F6F9</color>
</resources>
